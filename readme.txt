
The Test Driver for the StudentReg application. Imports it as as dependency and creates some mock data.

Creating and running the Executable jar

First clone the repo Driver In the project folder run the command

mvn clean package
Then change directory to the target folder and run this command

java -jar Driver-0.0.1-SNAPSHOT.jar